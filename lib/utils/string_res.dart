import 'package:flutter/material.dart';

class StringRes {
  static const favoriteText = Text(
    "Favorite",
    style: TextStyle(fontWeight: FontWeight.bold, fontSize: 18),
  );
  static const historyText = Text(
    "History",
    style: TextStyle(fontWeight: FontWeight.bold, fontSize: 18),
  );
}
